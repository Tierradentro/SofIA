/**
 * Origen de la asociación del código de barras (HU-011 escaneado /
 * HU-012 manual — el sistema registra el origen).
 */
export enum BarcodeOrigin {
  ESCANEADO = 'ESCANEADO',
  MANUAL = 'MANUAL',
}
