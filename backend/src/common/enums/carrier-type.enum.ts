/**
 * Tipo de transportadora (HU-008): externa (con guía) o interna
 * (transporte propio, sin guía externa obligatoria, M09 paso 5).
 */
export enum CarrierType {
  EXTERNA = 'EXTERNA',
  INTERNA = 'INTERNA',
}
