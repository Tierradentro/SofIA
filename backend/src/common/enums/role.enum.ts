/**
 * Roles del sistema (Spec §4). Un usuario tiene un solo rol.
 */
export enum Role {
  OPERADOR = 'OPERADOR',
  GENERADOR = 'GENERADOR',
  ADMINISTRADOR = 'ADMINISTRADOR',
  COMERCIAL = 'COMERCIAL',
  API = 'API',
}
